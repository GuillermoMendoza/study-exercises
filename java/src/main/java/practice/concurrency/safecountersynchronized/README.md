# Safe counter with `synchronized`

Implement `SynchronizedSafeCounter` using a plain `int` protected by Java's `synchronized` monitor mechanism. Both `increment()` and `value()` must be safe when called by many threads. Do not use `AtomicInteger` in this exercise.

Run it with:

```sh
./practice test safe-counter-synchronized
```
